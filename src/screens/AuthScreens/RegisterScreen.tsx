import React from "react";
import { ViewProps, View, Text } from "react-native";
const RegisterScreen = (props: ViewProps) => {
  return (
    <View>
      <Text>
        màn register này
      </Text>
    </View>
  )
}

export default RegisterScreen