import React from 'react';
import {TouchableOpacity} from 'react-native';
const MedicineItem = ({item}: MedicineItemProps) => {
  return <TouchableOpacity></TouchableOpacity>;
};
export default MedicineItem;

interface MedicineItemProps {
  item: any;
}
